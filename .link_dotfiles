#! /bin/bash

for f in $HOME/.*; do
  if [ -h $f ]; then
    echo "rm $f"
    rm $f
  fi
done

dir=$(pwd)
for f in *; do
  echo "$f"
  [ -e "$HOME/.$f" ] && rm "$HOME/.$f"
  ln -s "$dir/$f" "$HOME/.$f"
done
