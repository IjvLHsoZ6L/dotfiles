#! /bin/bash

echo sudo apt update
sudo apt update
echo

echo sudo apt -y dist-upgrade
sudo apt -y dist-upgrade
echo

echo sudo apt -y autoremove
sudo apt -y autoremove
echo

echo sudo apt autoclean
sudo apt autoclean
echo

echo Done
